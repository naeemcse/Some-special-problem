#include<stdio.h>
int main()
{
    int fibo,i=0,first=0,second=1;
    int n;
    scanf("%d",&n);
    do{
        if(i<=1)
            fibo=i;

        else
            {
        fibo=first+second;
        first=second;
        second=fibo;
        }
 i++;
 printf("%d\t",fibo);

    }while(i<n);


}
